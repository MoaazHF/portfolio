// Content lives in profile.json — edit it with tools/profile-form.html (open the file in a browser).
// Empty image/rive paths render styled placeholders.
import data from './profile.json';

export const profile = data;
